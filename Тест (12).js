1)
//в чем + стрелочных функций?
// они укорачивают код
2)
//превратите функцию ниже в стрелочную
newUser = (name, age) =>  {
    if (age > 18) {
        console.log(`${name} ты стар`);
    } else {
        console.log(`${name} ты сликом мал`);
    } 

}

3)
//сделай скрипт ниже короче
call = (a,b) => return a + b
// 56 bonus

function sortrovka(arr1) {
    return arr1.filter()
}
